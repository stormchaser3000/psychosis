#include <iosteam>
#include "main_menu.h"

// define WelcomeText member functions
WelcomeText::WelcomeText(int pos_x, int pos_y, bool visible) {
  position.x = pos_x;
  position.y = pos_y;
  visible = is_shown
  // TODO: textures
  //image = LoadImage("")
  //texture = TextureFromImage(image)
}

WelcomeText::render() {
  
}
